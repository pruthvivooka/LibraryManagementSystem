1. To populate the database, use the following command.
   psql library_management_system < database.txt
2. Go the the settings.py file and update the paths and the authentication of the database fields.
3. Install Django admin if not already installed using pip
	pip install Django
4. To Run run the following command when in LibraryManagementSystem Folder
	python manage.py runserver
